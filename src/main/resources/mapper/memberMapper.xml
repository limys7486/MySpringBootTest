<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.memberMapper">

    <select id="selectMemberBoard" resultType="hashmap">
        <![CDATA[
            select username, real_name as realName, email, address, mobile, role_name as roleName, update_date as updateDate, enabled
            from tbl_member
        ]]>
    </select>



</mapper>